<% if cart_item == @current_item %>
  <tr id="current_item">
<% else %>
  <tr>
<% end %>
    <td colspan="2"><%= cart_item.quantity %></td>
    <td><%= h(cart_item.title) %></td>
    <td  colspan="2"class="item-price"><%= number_to_currency(cart_item.price, :unit => "$") %></td>
</tr>